FROM golang:1.22-alpine AS build
WORKDIR /src
COPY go.mod ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /out/mitm-hub .

FROM alpine:3.20
WORKDIR /app
COPY --from=build /out/mitm-hub /app/mitm-hub
EXPOSE 8090
VOLUME /pb_data
ENTRYPOINT ["/app/mitm-hub"]
