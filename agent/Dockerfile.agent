FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml ./
RUN pip install --no-cache-dir mitmproxy pocketbase websockets
COPY . .
ENTRYPOINT ["python", "mitm_agent.py", "--hub", "${HUB_URL}", "--token", "${TOKEN}"]
